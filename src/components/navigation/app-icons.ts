/**
 * The app's domain icon vocabulary.
 *
 * Keeping these aliases in one place makes the same concept look the same in
 * the desktop nav, mobile menus, empty states, and contextual actions.
 */
export {
  BadgeHelp as QuizIcon,
  BookOpenText as DexIcon,
  GalleryHorizontalEnd as SwipeIcon,
  GalleryVerticalEnd as CardsIcon,
  LibraryBig as SetsIcon,
  ListChecks as ListsIcon,
  MapPinned as LocationsIcon,
  Scale as CompareIcon,
  UsersRound as TeamsIcon,
  WalletCards as DecksIcon,
} from "lucide-react";
